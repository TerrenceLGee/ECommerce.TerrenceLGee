<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="ECommerce.AvaloniaClient.TerrenceLGee.MainUserView"
             xmlns:vm="using:ECommerce.AvaloniaClient.TerrenceLGee.ViewModels"
             x:DataType="vm:MainUserViewModel">
  <Grid ColumnDefinitions="350, *">
	  <Border Grid.Column="0" Background="#333333" Padding="10">
		  <ListBox ItemsSource="{Binding MenuItems}"
                   SelectedItem="{Binding SelectedMenuItem}"
                   Background="Transparent"
                   BorderThickness="0">
              <ListBox.ItemTemplate>
				  <DataTemplate>
					  <TextBlock Text="{Binding DisplayName}" Margin="5"/>
                  </DataTemplate>
              </ListBox.ItemTemplate>
          </ListBox>
      </Border>

      <Border Grid.Column="1" Padding="20">
		  <ContentControl Content="{Binding CurrentSubView}"/>
      </Border>
  </Grid>
</UserControl>
